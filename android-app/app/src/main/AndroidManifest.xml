<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Lets the app check whether an on-device speech-recognition service exists before it
         offers the mic button (see SpeechRecognizerController). Required on Android 11+. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".LangCalcApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.LangCalc"
        android:supportsRtl="true">

        <!-- GPU backend is not used by the fine-tuned FunctionGemma checkpoint (it runs on CPU
             only, see EngineConfig in LangCalcEngine.kt), so no uses-native-library entries for
             libvndksupport.so / libOpenCL.so are needed here. Add them back if you enable
             Backend.GPU() for a future model. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LangCalc"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
